/**
 * Adds two 16-bit values, a, b and a carry-in, c together.
 * The most significant carry bit is ignored.
 */

CHIP Add16C {
    IN a[16], b[16], c;
    OUT out[16];

    PARTS:
   // Put you code here:
}
